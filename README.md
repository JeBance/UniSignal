# 🚀 UniSignal Relay

**Промежуточный шлюз для нормализации и трансляции криптовалютных сигналов из Telegram**

UniSignal Relay подключается к [Telegrab](https://github.com/JeBance/telegrab), получает сообщения из криптовалютных Telegram-каналов, нормализует их в структурированный формат и транслирует авторизованным клиентам через WebSocket.

---

## 📋 Оглавление

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [⚡ Установка в один клик](#-установка-в-один-клик-docker)
- [Быстрый старт](#-быстрый-старт)
- [Конфигурация](#-конфигурация)
- [Admin API](#-admin-api)
- [WebSocket API](#-websocket-api)
- [Парсер сигналов](#-парсер-сигналов)
- [Разработка](#-разработка)
- [Структура проекта](#-структура-проекта)

---

## ✨ Возможности

- 🔌 **Подключение к Telegrab** — постоянное WebSocket-соединение с авто-переподключением
- 📡 **Фильтрация каналов** — белый список отслеживаемых Telegram-каналов в БД
- 🧠 **Умный парсер** — извлечение тикера, направления (LONG/SHORT), цен входа/выхода
- 💾 **Дедупликация** — защита от дубликатов при переподключении
- 🔐 **Аутентификация клиентов** — API-ключи для доступа к WebSocket
- 📊 **Admin HTTP API** — управление клиентами, каналами, статистика
- 🖥️ **Веб-интерфейс** — Dashboard, управление клиентами, каналами, живые сигналы
- 🔄 **Буферизация** — сохранение сообщений при недоступности БД
- 📝 **Логирование** — структурированные логи через Pino

---

## 🏗 Архитектура

```
┌─────────────┐      WebSocket       ┌──────────────────┐
│   Telegrab  │ ───────────────────► │  UniSignal Relay │
│  (Upstream) │    ws://194.87...    │   (Port 3001)    │
└─────────────┘                      └────────┬─────────┘
                                              │
                    ┌─────────────────────────┼─────────────────────────┐
                    │                         │                         │
              ┌─────▼─────┐           ┌──────▼──────┐           ┌──────▼──────┐
              │   Admin   │           │   Client    │           │  PostgreSQL │
              │ HTTP API  │           │  WebSocket  │           │   Database  │
              │ /admin/*  │           │    /ws      │           │  (messages, │
              └───────────┘           └─────────────┘           │   channels, │
                                                                │   clients)  │
                                                                └─────────────┘
```

### Поток данных

1. **Telegrab** отправляет событие `new_message` →
2. **UniSignal** проверяет канал в белом списке →
3. **Парсер** извлекает данные (тикер, направление, цены) →
4. **Дедупликация** по `unique_hash` →
5. **Сохранение** в PostgreSQL →
6. **Трансляция** авторизованным клиентам через WebSocket

---

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Node.js 18+ (для локальной разработки)

---

## ⚡ Установка в один клик (Docker)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/JeBance/UniSignal.git && cd UniSignal

# 2. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env: указать TELEGRAB_API_KEY и TELEGRAB_WS_URL

# 3. Запустить
docker compose up -d

# 4. Проверить
curl http://localhost:3001/health
```

> **Примечание:** Миграции базы данных применяются автоматически при первом запуске.

---

## 📝 Подробная инструкция

### 1. Клонирование репозитория

```bash
git clone https://github.com/JeBance/UniSignal.git
cd UniSignal
```

### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```bash
# App
PORT=3001
LOG_LEVEL=info
ADMIN_MASTER_KEY=super_secret_admin_key_change_me

# Telegrab
# Если Telegrab на том же сервере: ws://host.docker.internal:3000/ws
# Если Telegrab на удалённом сервере: ws://<ip>:3000/ws
TELEGRAB_WS_URL=ws://host.docker.internal:3000/ws
TELEGRAB_API_KEY=<your_telegrab_api_key>

# Database
DATABASE_URL=postgresql://unisignal:unisignal_password@db:5432/unisignal
```

### 3. Запуск через Docker Compose

```bash
docker compose up -d
```

> **Примечание:** Миграции базы данных применяются автоматически при каждом запуске.

Проверка логов:

```bash
docker compose logs -f app
```

### 4. Проверка работоспособности

```bash
curl http://localhost:3001/health
```

Ответ:

```json
{
  "status": "ok",
  "service": "UniSignal Relay",
  "timestamp": "2026-02-26T12:00:00.000Z",
  "checks": {
    "database": "ok"
  }
}
```

---

## 🖥️ Веб-интерфейс

UniSignal включает современный веб-интерфейс для управления системой.

### Запуск через Docker

```bash
docker compose up -d
```

Веб-интерфейс будет доступен по адресу: **http://localhost:3001/ui**

### Локальная разработка

```bash
cd frontend
npm install
npm run dev
```

Веб-интерфейс в режиме разработки: **http://localhost:3000**

### Возможности

- **📊 Dashboard** — статистика сообщений, сигналов, клиентов
- **📡 Сигналы** — живая трансляция сигналов через WebSocket
- **👥 Клиенты** — создание и управление API-ключами
- **📺 Каналы** — управление белым списком Telegram-каналов

---

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Обязательна | Описание | По умолчанию |
|------------|-------------|----------|--------------|
| `PORT` | Нет | Порт для HTTP и WebSocket | `3001` |
| `LOG_LEVEL` | Нет | Уровень логирования | `info` |
| `ADMIN_MASTER_KEY` | **Да** | Мастер-ключ для Admin API | — |
| `TELEGRAB_WS_URL` | **Да** | URL WebSocket Telegrab | — |
| `TELEGRAB_API_KEY` | **Да** | API-ключ для Telegrab | — |
| `DATABASE_URL` | **Да** | Connection string PostgreSQL | — |

> **Примечание для Docker:** Если Telegrab запущен на хост-машине, используйте `ws://host.docker.internal:3000/ws`. На Linux может потребоваться добавить `--add-host=host.docker.internal:host-gateway` к docker-compose.

### Конфигурация парсеров

Файл `config/parsers.yaml` содержит паттерны для извлечения данных из сообщений:

```yaml
channels:
  default:
    direction:
      long: ["⬆️", "🟢", "🚀", "Long", "BUY"]
      short: ["⬇️", "🔴", "📉", "Short", "SELL"]
    ticker:
      pattern: "/\\b([A-Z]{3,6})(USDT|BTC|ETH)\\b/"
      group: 1
    entry:
      pattern: "/(?:Entry|Вход)[:\\s]*([0-9.,]+)/i"
      group: 1
    stop_loss:
      pattern: "/(?:SL|Stop)[:\\s]*([0-9.,]+)/i"
      group: 1
    take_profit:
      pattern: "/(?:TP|Target)[:\\s]*([0-9.,]+)/i"
      group: 1
```

---

## 🔧 Admin API

Все эндпоинты требуют заголовок `X-Admin-Key` со значением `ADMIN_MASTER_KEY`.

### Управление клиентами

#### POST /admin/clients

Создать нового клиента:

```bash
curl -X POST http://localhost:3001/admin/clients \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

Ответ:

```json
{
  "id": "uuid-...",
  "api_key": "usk_abc123...",
  "is_active": true,
  "created_at": "2026-02-26T12:00:00.000Z"
}
```

#### GET /admin/clients

Получить список всех клиентов:

```bash
curl http://localhost:3001/admin/clients \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

#### DELETE /admin/clients/:id

Отозвать ключ клиента:

```bash
curl -X DELETE http://localhost:3001/admin/clients/<client-id> \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

### Управление каналами

#### POST /admin/channels

Добавить канал в белый список:

```bash
curl -X POST http://localhost:3001/admin/channels \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY" \
  -H "Content-Type: application/json" \
  -d '{"chat_id": 5610580916, "name": "JeBance", "is_active": true}'
```

#### GET /admin/channels

Получить список активных каналов:

```bash
curl http://localhost:3001/admin/channels \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

Получить все каналы (включая неактивные):

```bash
curl "http://localhost:3001/admin/channels?all=true" \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

#### DELETE /admin/channels/:chatId

Убрать канал из фильтра:

```bash
curl -X DELETE http://localhost:3001/admin/channels/5610580916 \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

#### PATCH /admin/channels/:chatId/toggle

Включить/выключить канал:

```bash
curl -X PATCH http://localhost:3001/admin/channels/5610580916/toggle \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY" \
  -H "Content-Type: application/json" \
  -d '{"is_active": false}'
```

### Статистика

#### GET /admin/stats

```bash
curl http://localhost:3001/admin/stats \
  -H "X-Admin-Key: $ADMIN_MASTER_KEY"
```

Ответ:

```json
{
  "messages": {
    "total": 150,
    "today": 25,
    "with_ticker": 120,
    "long_count": 80,
    "short_count": 40
  },
  "channels": {
    "active": 3
  },
  "clients": {
    "total": 5,
    "active": 4
  }
}
```

---

## 🔗 WebSocket API

### Подключение

```
ws://localhost:3001/ws
```

### Аутентификация

После подключения клиент **обязан** отправить сообщение в течение 5 секунд:

```json
{
  "action": "auth",
  "api_key": "usk_abc123..."
}
```

#### Успешная аутентификация

```json
{
  "status": "authenticated",
  "message": "Welcome to UniSignal Relay"
}
```

#### Ошибка аутентификации

```json
{
  "status": "error",
  "message": "Invalid API Key"
}
```

Соединение закрывается с кодом `4002`.

### Получение сигналов

После аутентификации клиент получает сигналы в формате:

```json
{
  "type": "signal",
  "data": {
    "id": 105,
    "channel": "JeBance",
    "direction": "LONG",
    "ticker": "BTCUSDT",
    "entryPrice": 45000,
    "stopLoss": 44000,
    "takeProfit": 47000,
    "text": "🟢 LONG BTCUSDT\nEntry: 45000\nSL: 44000\nTP: 47000",
    "timestamp": 1740569076
  }
}
```

### Пример клиента на JavaScript

```javascript
const ws = new WebSocket('ws://localhost:3001/ws');

ws.on('open', () => {
  console.log('Connected, authenticating...');
  ws.send(JSON.stringify({
    action: 'auth',
    api_key: 'usk_abc123...'
  }));
});

ws.on('message', (data) => {
  const message = JSON.parse(data.toString());
  
  if (message.status === 'authenticated') {
    console.log('✅ Authenticated:', message.message);
  } else if (message.type === 'signal') {
    console.log('📡 New signal:', message.data);
  }
});

ws.on('close', (code, reason) => {
  console.log(`Disconnected: code=${code}, reason=${reason}`);
});

ws.on('error', (err) => {
  console.error('WebSocket error:', err);
});
```

---

## 🧠 Парсер сигналов

### Поддерживаемые типы сигналов

UniSignal использует умный парсер для извлечения данных из сообщений Telegram.

| Тип сигнала | Источник | Описание | Приоритет |
|-------------|----------|----------|-----------|
| **Strong Signal** 🔴🔴🟢🟢 | VasyaBTC | Сильные сигналы с паттернами разворота | 1 (высший) |
| **Medium Signal** 🔴🟢 | VasyaBTC | Средние сигналы с техническим анализом | 2 |
| **Entry Signal** 📊 | VasyaBTC | Сигналы с Entry/Targets/Stop | 2 |
| **Quick Target** 🎯 | VasyaBTC (RU) | Быстрые цели на русском | 2 |
| **SENTIMENT** 📈 | VasyaBTC | Сентимент-анализ по таймфреймам | 4 |
| **Funding Rate** 💰 | ASFunding_bot | Ставки фандинга | 3 |

### Извлекаемые данные

**Базовые:**
- Тикер (например, `BTCUSDT`)
- Биржа (`BINANCE`, `BYBIT`, `MEXC`, `BATS`)
- Направление (`long`, `short`, `neutral`)

**Технические:**
- Таймфрейм (`5min`, `15min`, `1h`, `4h`, `12h`, `1d`)
- RSI и сигнал (`oversold`, `overbought`, `neutral`)
- Паттерн (`trend_reversal`, `ob_reversal`, `os_reversal`)
- Сила паттерна (%)

**Торговые:**
- Цена входа (`entry_price`)
- Цели (`targets` — массив)
- Стоп-лоссы (`stop_0_5`, `stop_1`)
- Ожидаемая прибыль

**Фандинг:**
- Ставка (%)
- Получатели (`longs`/`shorts`)
- Рекомендация (`long`/`short`)

### Confidence Score

Каждый сигнал получает оценку уверенности от 0 до 100:

**Повышают score:**
- +20 — Strong Signal
- +15 — Паттерн >50%
- +10 — RSI в экстремальной зоне (>70 или <30)
- +10 — Есть Entry/Targets/Stop

**Понижают score:**
- -10 — Паттерн <30%
- -10 — Нет стоп-лосса

### Примеры парсинга

| Текст сообщения | Результат |
|-----------------|-----------|
| `#BTCUSDT #StrongSignal ... 🔴🔴**↑ TREND Reversal ↓** 63% RSI: 67 ... SHORT` | `type: strong_signal, ticker: BTCUSDT, direction: short, rsi: 67, pattern_strength: 63` |
| `🟢 LONG BTCUSDT Entry: 45000 SL: 44000 TP: 47000` | `type: entry_signal, ticker: BTC, direction: long, entry: 45000, sl: 44000, tp: 47000` |
| `⚡️ Сигнал по фандингу (BYBIT) ... Ставка: -0.6000% ... Лонги получают` | `type: funding_rate, rate: -0.6, receiver: longs, action: long` |

### Формат выходных данных

Парсер возвращает стандартизированный JSON согласно [JSON Schema](docs_for_analysis/trading_signal_schema.json).

📖 **Полная документация:** См. [docs/USER_GUIDE.md](docs/USER_GUIDE.md)

---

## 🛠 Разработка

### Локальная разработка с Docker

Для разработки с hot reload используйте `docker-compose.override.yml`:

```bash
cp docker-compose.override.example docker-compose.override.yml
# Отредактируйте TELEGRAB_API_KEY в docker-compose.override.yml
docker compose -f docker-compose.yml -f docker-compose.override.yml up -d
```

Это включит:
- Hot reload (tsx watch)
- Логирование уровня debug
- Volume для исходного кода

### Локальный запуск (без Docker)

```bash
# Установка зависимостей
npm install

# Запуск в режиме разработки (hot reload)
npm run dev

# Сборка TypeScript
npm run build

# Запуск продакшн-версии
npm start
```

### Тесты

```bash
# Запуск всех тестов
npm test

# Запуск в режиме watch
npm run test:watch
```

### Миграции БД

```bash
# Применить все миграции
npm run db:migrate:up

# Откатить последнюю миграцию
npm run db:migrate:down

# Переделать последнюю миграцию
npm run db:migrate:redo

# Создать новую миграцию
npm run db:migrate create my_migration
```

### Линтинг

```bash
npm run lint
```

---

## 📁 Структура проекта

```
UniSignal/
├── src/
│   ├── index.ts                    # Entry point
│   ├── config/
│   │   └── parsers.yaml            # Конфигурация парсеров
│   ├── db/
│   │   ├── connection.ts           # Подключение к PostgreSQL
│   │   ├── migrations/
│   │   │   ├── 001_initial_schema.ts
│   │   │   └── db.config.ts
│   │   └── repositories/
│   │       ├── channel-repository.ts
│   │       ├── client-repository.ts
│   │       └── message-repository.ts
│   ├── services/
│   │   ├── admin-api.ts            # HTTP Admin API
│   │   ├── buffer.ts               # In-memory буфер
│   │   ├── client-ws.ts            # Downstream WebSocket
│   │   ├── message-processor.ts    # Обработчик сообщений
│   │   ├── parser.ts               # Парсер сигналов
│   │   └── telegrab-ws.ts          # Upstream Telegrab WS
│   └── utils/
│       └── logger.ts               # Pino логгер
├── tests/
│   ├── admin-api.test.ts
│   ├── client-ws.test.ts
│   └── parser.test.ts
├── config/
│   └── parsers.yaml
├── scripts/
│   ├── migrate.sh
│   └── migrate.cmd
├── docker-compose.yml
├── Dockerfile
├── package.json
├── tsconfig.json
└── README.md
```

---

## 📊 Критерии приемки

- ✅ `docker-compose up -d` поднимает систему без ошибок
- ✅ Можно создать клиента и добавить канал через HTTP API
- ✅ Сообщение в Telegram → появляется у клиента WS < 1 сек
- ✅ Сообщения из каналов не из белого списка игнорируются
- ✅ Перезапуск сервиса не создаёт дублей в БД
- ✅ Клиент без ключа не получает данные
- ✅ Admin API без ключа недоступен

---

## 📝 Лицензия

MIT

---

## 👤 Автор

JeBance

[GitHub](https://github.com/JeBance/UniSignal)
