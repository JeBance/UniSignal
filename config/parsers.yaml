# Конфигурация парсеров для различных каналов
# Каждый канал может иметь свои паттерны для извлечения данных

channels:
  # Шаблон для универсального парсинга
  default:
    direction:
      long: ["⬆️", "🟢", "🚀", "Long", "LONG", "Buy", "BUY", "Лонг", "ЛОНГ", "Покупка"]
      short: ["⬇️", "🔴", "📉", "Short", "SHORT", "Sell", "SELL", "Шорт", "ШОРТ", "Продажа"]
    ticker:
      pattern: "/\\b([A-Z]{3,6})(USDT|BTC|ETH|USD|BUSD)\\b/"
      group: 1
    entry:
      pattern: "/(?:Entry|Вход|Enter|Open)[:\\s]*([0-9.,]+)/i"
      group: 1
    stop_loss:
      pattern: "/(?:Stop Loss|Stop|SL|Стоп|СЛ)[:\\s]*([0-9.,]+)/i"
      group: 1
    take_profit:
      pattern: "/(?:Take Profit|TP|Target|Тейк|ТП|Цель)[:\\s]*([0-9.,]+)/i"
      group: 1

  # Специфичные настройки для конкретных каналов (переопределяют default)
  # Пример для VasyaBTC-Signals:
  # -1002678035223:
  #   direction:
  #     long: ["🟢", "НОВАЯ ЦЕЛЬ РОСТА"]
  #     short: ["🔴", "НОВАЯ ЦЕЛЬ СНИЖЕНИЯ"]
