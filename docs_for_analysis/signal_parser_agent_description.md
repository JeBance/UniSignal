# Описание для ИИ-агента: Парсер Telegram-сигналов

## 🎯 Назначение

Агент предназначен для парсинга, нормализации и стандартизации торговых сигналов из различных Telegram-каналов с последующей отправкой клиентам через WebSocket.

## 📋 Источники сигналов

| Канал | Тип сигналов | Особенности |
|-------|-------------|-------------|
| **VasyaBTC-Signals 🟢🔴** | Технические сигналы (Strong/Medium/SENTIMENT) | RSI, паттерны, уровни |
| **ASFunding_bot** | Funding Rate сигналы | Ставки фандинга BYBIT/MEXC |
| **JeBance** | Дубликаты + быстрые цели | RU/EN версии |
| **AutoScalping** | Информационные/рекламные | Фильтровать |

---

## 🔍 Типы сигналов для парсинга

### 1. StrongSignal / MediumSignal (VasyaBTC)

**Шаблон текста:**
```
#TICKER #SignalType
EXCHANGE, YYYY-M-D, TH:M:S UTC

**Ticker:** TICKER,  **TIMEFRAME
✔️Project, Country, Company** (опционально)

🔴/🟢**↑/↓ PATTERN ↓/↑**  PERCENT%   **RSI:** VALUE
⚠️Buying's/Selling's risky – **potential** LONG/SHORT📗/📕

**Last price:** PRICE
```

**Извлекаемые поля:**
- `ticker` — из #TICKER или **Ticker:**
- `signal_type` — StrongSignal / MediumSignal
- `exchange` — BINANCE, BATS
- `timestamp` — преобразовать в ISO8601
- `timeframe` — 5 min, 15 min, 1h, 4h, 12h, D
- `pattern` — TREND Reversal / OB Reversal / OS Reversal
- `pattern_direction` — ↑↓ (вверх/вниз)
- `pattern_strength` — процент
- `rsi` — числовое значение
- `direction` — LONG (🟢📗) / SHORT (🔴📕)
- `last_price` — цена

**Сила сигнала:**
- `StrongSignal` → 🔴🔴 / 🟢🟢 (двойные)
- `MediumSignal` → 🔴 / 🟢 (одинарные)

---

### 2. SENTIMENT (VasyaBTC)

**Шаблон текста:**
```
#TICKER #SENTIMENT
EXCHANGE, YYYY-M-D, T23:59:0 UTC

**Ticker:** TICKER  
                  **Day** CHANGE% / **24h** CHANGE%

→/▲/▼**🟩/🟥 ZONE** PERCENT%  /  RSI - **TIMEFRAME**
...

**Last price:** PRICE
```

**Извлекаемые поля:**
- `ticker`, `exchange`, `timestamp`, `last_price`
- `day_change` — изменение за день (%)
- `24h_change` — изменение за 24ч (%)
- `sentiment_zones` — массив по таймфреймам:
  - `timeframe`
  - `zone` — OS (Oversold) / OB (Overbought)
  - `zone_percent`
  - `rsi`
  - `trend_arrow` — → (neutral), ▲ (up), ▼ (down)

---

### 3. Entry Signal с Targets (VasyaBTC)

**Шаблон текста:**
```
#TICKER
🔴/🟢EXCHANGE, YYYY-M-D, TH:M:S UTC

**Ticker:** TICKER,  **TIMEFRAME
✔️Project info**

**Entry:** PRICE
**Targets:** PRICE1 - PRICE2
**Stop:** **0.5%** - PRICE,  **1%** - PRICE

**Expected profit:** VALUE
PERCENT% to target
```

**Извлекаемые поля:**
- `entry_price`
- `targets` — массив [price1, price2]
- `stop_loss_0.5` — стоп при 0.5%
- `stop_loss_1` — стоп при 1%
- `expected_profit`
- `progress_to_target`
- `direction` — по эмодзи (🔴=SHORT, 🟢=LONG)

---

### 4. Quick Target (VasyaBTC, RU)

**Шаблон текста:**
```
YYYY-MM-DDTHH:MM:SSZ, EXCHANGE 

**Тикер:** TICKER  

**Таймфрейм:** N минут

🟢 **НОВАЯ ЦЕЛЬ РОСТА** 🟢 / 🔴 **НОВАЯ ЦЕЛЬ СНИЖЕНИЯ** 🔴

**Вход:** PRICE 

**Тейки:** PRICE1-PRICE2 
```

**Извлекаемые поля:**
- `ticker`, `exchange`, `timestamp`
- `timeframe` — преобразовать "5 минут" → "5min"
- `direction` — РОСТА=LONG, СНИЖЕНИЯ=SHORT
- `entry_price`
- `targets` — массив

---

### 5. Funding Rate Signal (ASFunding_bot)

**Шаблон текста:**
```
⚡️ **Сигнал по фандингу (EXCHANGE)**
🔹 **Инструмент:** [TICKER](URL)
🕒 **Время:** DD.MM.YYYY HH:MM
💰 **Ставка:** -X.XXXX%
📈 Лонги получают / 📉 Шорты получают
🟢 Открывать ЛОНГ / 🔴 Открывать ШОРТ
```

**Извлекаемые поля:**
- `exchange` — BYBIT / MEXC
- `instrument` — тикер (извлечь из [TICKER](URL))
- `funding_time` — преобразовать в ISO8601
- `funding_rate` — процент (отрицательный = лонги получают)
- `receiver` — "longs" / "shorts"
- `recommended_action` — LONG / SHORT
- `trading_link` — URL

**Логика:**
- Отрицательная ставка (−) → лонги получают → рекомендация LONG
- Положительная ставка (+) → шорты получают → рекомендация SHORT

---

## 🧹 Нормализация данных

### Преобразование таймфреймов
| Исходный | Нормализованный |
|----------|----------------|
| `5 min`, `5 минут` | `5min` |
| `15 min`, `15 минут` | `15min` |
| `1h` | `1h` |
| `4h` | `4h` |
| `12h` | `12h` |
| `D`, `Daily` | `1d` |

### Преобразование времени
| Формат | Пример | ISO8601 |
|--------|--------|---------|
| VasyaBTC | `2026-2-27, T10:0:0 UTC` | `2026-02-27T10:00:00Z` |
| Quick Target | `2026-02-27T08:00:01Z` | `2026-02-27T08:00:01Z` |
| Funding | `27.02.2026 13:00` | `2026-02-27T13:00:00Z` |

### Нормализация направлений
| Сигнал | Направление |
|--------|-------------|
| 🟢, 📗, РОСТА, LONG | `long` |
| 🔴, 📕, СНИЖЕНИЯ, SHORT | `short` |
| 🟢🟢, 🔴🔴 | `strong_long` / `strong_short` |

---

## 🚫 Фильтрация

**Игнорировать сообщения:**
- Рекламные/промо (AutoScalping подписка)
- Личные сообщения без сигналов
- Файлы без текстового сигнала
- Сообщения без тикера

**Проверка валидности:**
```python
def is_valid_signal(parsed):
    required = ['ticker', 'exchange', 'signal_type']
    if not all(parsed.get(f) for f in required):
        return False
    if parsed.get('signal_type') in ['StrongSignal', 'MediumSignal', 'QuickTarget', 'FundingRate']:
        if not parsed.get('direction'):
            return False
    return True
```

---

## 📊 Приоритеты сигналов

| Тип | Приоритет | WebSocket priority |
|-----|-----------|-------------------|
| StrongSignal | 1 (высший) | 1 |
| MediumSignal | 2 | 2 |
| QuickTarget | 2 | 2 |
| FundingRate | 3 | 3 |
| SENTIMENT | 4 | 4 |
